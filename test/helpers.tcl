# Minimal helpers.tcl for tns_path_group test
# This file can be populated with common Tcl procedures if needed.

# Example of how read_liberty might be defined if it were not built-in
# proc read_liberty { args } {
#   eval sta::read_liberty $args
# }

# Example of how read_verilog might be defined if it were not built-in
# proc read_verilog { args } {
#  eval sta::read_verilog $args
# }

# Example of how link_design might be defined if it were not built-in
# proc link_design { args } {
#   eval sta::link_design $args
# } 